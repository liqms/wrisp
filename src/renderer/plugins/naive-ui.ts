import { create } from 'naive-ui'

// 创建 Naive UI 实例
const naive = create({
  // 可以在这里添加全局配置
  // themeOverrides: {
  //   common: {
  //     primaryColor: '#2080f0',
  //     primaryColorHover: '#4098fc',
  //     primaryColorPressed: '#1060c9',
  //   }
  // }
})

export default naive